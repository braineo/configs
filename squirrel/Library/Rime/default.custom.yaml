# Rime default settings
# encoding: utf-8

patch:
  schema_list:
    - schema: wubi86
    - schema: terra_pinyin
  # override default keybinding for less used ascii mode
  ascii_composer:
    switch_key:
      Caps_Lock: noop
      Control_L: noop
      Control_R: noop
      Eisu_toggle: noop
      Shift_L: inline_ascii
      Shift_R: commit_code
